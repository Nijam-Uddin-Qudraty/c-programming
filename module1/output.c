#include<stdio.h>
int main()
{
    int rahim, karim;
    char c;
    float f; 
    scanf("%d%% %da %c %f", &rahim, &karim, &c, &f);
    printf("%d%% %da %c %f", rahim, karim, c, f);
    return 0;

    //     define type at first and btween " " and then
//     use comma and us '&'(empharsion) sign with write varibale names.
// type %% for inputing a percentage sign with integer. also need to type it on print funtion and must include % sing in input.
// input anyting extra like this % sing
}